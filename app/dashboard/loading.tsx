export default function DashboardLoading() {
  return (
    <div
      style={{
        minHeight: 200,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#8aa0c0",
        fontFamily: "var(--font-dm-mono), monospace",
        fontSize: 14,
      }}
    >
      Loading…
    </div>
  );
}
